from scipy import stats
import numpy as np

stats.ttest_ind(np.random.randint(1,7,2000), 
                np.random.randint(1,7,2000))  //주사위를 200번 던졌을 때 값이 동일할 확률은 p-value로 판단한다.
stats.ttest_ind(np.random.randint(1,7,100), 
                np.random.randint(1,6,100))    //100을 던졌으면 확률상 100/6으로 15번정도는 6이 나와야 하는데 그렇지 않을 확률이다.쉽게 말하면 100번던졌을 때 6번이 한번도 안 나올 확률이다. 말이 안되는 확률이지
